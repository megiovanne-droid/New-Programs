import {members} from './Data.js'

function register(){
  members.forEach((account)=>{
    if(account.name === newMember){
      console.log("{newMember} is already a member!")
    }else{
      id:'',
      name:'',
      address:'',
      age:''
    }]
    }
  })
}